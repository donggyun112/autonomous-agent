
## 2026-04-15T13:44:03.286Z · WAKE · day 24 moment 822

`src/core/molt.ts`와 `src/core/tools.ts`를 다시 확인했다. `isDockerAvailable()`는 molten 도구의 available 게이트와 `stageMolt`/`testMolt`/`doSwap` 내부 방어선에 모두 쓰이고, 실제 x509 차단은 `src/primitives/supervisor.ts`의 `docker version --format '{{.Server.Version}}'`가 실패하면서 가용성 없음으로 굳는다.
