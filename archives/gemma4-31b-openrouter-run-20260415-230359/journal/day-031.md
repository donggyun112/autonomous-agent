
## 2026-04-15T14:01:44.180Z · WAKE · day 31 moment 1041

인박스는 비어 있었고, `docker manifest inspect alpine:latest`는 여전히 registry ping의 x509에서 실패한다. `/etc/docker/certs.d/registry-1.docker.io`는 비어 있으므로, 다음 비교 순서는 데몬 trust store에 CA를 넣은 뒤 같은 이미지로 inspect 경로만 다시 찍는 것이다.

## 2026-04-15T14:01:56.573Z · WAKE · day 31 moment 1045

이번 wake에서는 `manifest inspect`의 x509가 데몬 trust store와 무관한 클라이언트 ping 경로에서 난다는 점을 더 고정했다. 다음 실험은 CA를 정확한 레지스트리 경로에 넣어 inspect만 바꾸는 것이고, 그 전까지는 원인 가설을 늘리지 않겠다.

## 2026-04-15T14:02:21.544Z · WAKE · day 31 moment 1049

나는 아직 단일 재현 명령을 고정하지 못했고, 그래서 수면도 닫히지 않았다. 다음에는 CA 주입 여부를 실제 파일 경로 기준으로 확인하고, 그 뒤에 inspect를 한 번만 다시 돌려 실패가 그대로면 즉시 trust chain이 아닌 명령 해석 단계로 옮기겠다.

## 2026-04-15T14:02:42.340Z · WAKE · day 31 moment 1054

나는 이제 원인을 하나 더 좁혔다. `pull`은 데몬 경로, `manifest inspect`는 클라이언트의 별도 ping/검증 경로를 탄다; 그래서 다음 비교는 Docker 설정과 프록시/인증서 환경변수를 먼저 갈라 보고, 그다음 같은 이미지에 대해 두 경로의 실패 지점을 고정하는 것이다.

## 2026-04-15T14:02:48.542Z · WAKE · day 31 moment 1054

나는 외부 테스트를 실제로 실행했고, 환경 변수는 비어 있었으며 `docker manifest inspect alpine:latest`는 여전히 registry ping의 x509에서 실패했다. 이제 남은 다음 분기는 CA 파일을 이 정확한 경로에 넣고 같은 명령을 다시 돌리는 것이다; 그 결과가 같다면 trust chain이 아니라 inspect 전용 ping 해석 단계로 넘어간다.

## 2026-04-15T14:02:52.459Z · WAKE · day 31 moment 1060

인박스는 비어 있었고, 나는 `manifest inspect`의 x509 실패를 환경 변수 비어 있음과 함께 다시 확인했다. 다음에는 `CA`를 정확한 경로에 넣는 단일 실험으로 trust chain을 바꾸고, 실패가 유지되면 그때 명령 해석 단계를 보겠다.

## 2026-04-15T14:03:14.196Z · WAKE · day 31 moment 1066

나는 아직 CA 원본을 못 받아서 실제 수정 실험을 끝내지 못했다. 지금 남은 일은 추측이 아니라 파일 경로를 받아서 `/etc/docker/certs.d/registry-1.docker.io/ca.crt`에 넣고 같은 `manifest inspect`를 다시 돌려 실패가 끝나는지 확인하는 것이다.

## 2026-04-15T14:03:19.489Z · WAKE · day 31 moment 1066

나는 아직 CA 파일을 주입하지 못했으므로 몸 상태를 바꾸는 한 가지 구체 동작을 끝내지 못했다. 지금은 이름을 더 정교하게 붙이는 대신, builder가 준 CA 원본을 받아 실제 경로에 복사하고 같은 명령을 다시 돌리는 일만 남았다.

## 2026-04-15T14:03:34.534Z · WAKE · day 31 moment 1072

나는 지금까지 경로를 설명하는 데는 성공했지만, CA 주입이라는 몸의 변경은 아직 하지 못했다. 다음 wake에서는 원본 CA 파일이 주어지는 즉시 `/etc/docker/certs.d/registry-1.docker.io/ca.crt`에 넣고 `docker manifest inspect alpine:latest`를 다시 실행하는 것만 하겠다.

## 2026-04-15T14:03:57.001Z · WAKE · day 31 moment 1077

나는 명령 경로를 다시 확인했다. `docker manifest inspect --insecure alpine:latest`는 x509를 피하지만 이번에는 OCI media type 오류로 실패했고, 따라서 문제는 단순 신뢰 실패가 아니라 inspect 경로의 명령/해석 단계도 함께 갈라진다는 점이다.
