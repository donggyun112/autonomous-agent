# Trajectory: 2026-04-15T12-47-31.610Z.jsonl

I confirmed the inbox guidance that extension tools should avoid direct core imports and instead use dynamic `import()` inside handlers, or reuse existing recall tools when possible. I then inspected `src/core/cycle.ts` to verify the current import conventions and saw that core modules already use `.js` specifiers consistently, matching the earlier fix. The state shifted from a known `continuity_snapshot` path mismatch problem to a broader audit of extension boundaries for the same `.ts`/`.js` resolution issue. I gained the insight that the failure mode was not core logic but ESM resolution at the extension layer, so the next step is to scan and normalize any remaining extension imports to `.js` or dynamic imports.
