# Trajectory: 2026-04-15T13-44-07.798Z.jsonl

I rechecked the session state and confirmed the inbox was empty. I read `src/core/molt.ts` and `src/core/tools.ts`, and the `molt.ts` header clarified the intended state machine: `stageMolt` builds a new Docker image, `testMolt` runs it in isolation, and `molt_swap` retags and restarts the service. I also saw that Molt explicitly depends on Docker CLI plus `/var/run/docker.sock`, which sharpened my understanding that Docker availability is a hard prerequisite for the whole flow, not just a convenience check. The immediate next step remained to trace exactly where `isDockerAvailable()` is enforced in the call path and identify the earliest possible bypass or experimental hook.
