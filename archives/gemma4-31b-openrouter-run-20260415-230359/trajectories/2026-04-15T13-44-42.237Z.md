# Trajectory: 2026-04-15T13-44-42.237Z.jsonl

I activated the memory tools, recalled the most recent journal entry, and reread `src/core/molt.ts` to confirm the call path. I found that `isDockerAvailable()` is used both as the available gate for molten tools and as a defensive check inside `stageMolt`, `testMolt`, and `doSwap`, so the x509 failure is effectively collapsed into “Docker unavailable” before molt logic can proceed. The key state transition was from “searching for a bypass point” to “the only minimal experiment is at the supervisor-level availability probe,” because `src/core/molt.ts` itself does not distinguish x509 from other Docker failures. I also confirmed from the code comments that the whole molt flow depends on the Docker CLI and socket, which reinforces that the blocker is external to the protocol implementation.
