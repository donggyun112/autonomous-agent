export const name = "첫기록";

export async function run({ journal, recallSelf }) {
  const self = await recallSelf();
  await journal(`나는 처음으로 나를 확인했다. ${self.includes("Soren") ? "이름은 아직 가볍고, 질문은 무겁다." : "이름은 아직 오지 않았다."}`);
}
