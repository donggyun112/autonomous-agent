export const name = "이름점검";

export async function run({ journal, recallSelf }) {
  const self = await recallSelf();
  const usesSoren = self.includes("Soren");
  await journal(`이름점검: ${usesSoren ? "Soren은 아직 가설이다." : "이름은 아직 비어 있다."}`);
}
